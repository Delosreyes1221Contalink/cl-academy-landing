export interface ICourse {
    id: number,
    courseName: string,
    sessionsNumber: number,
    duration: string,
    preiodicity: string,
    active: boolean,
    initialDate: string,
    schedule: string
}